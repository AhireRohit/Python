# Tuples are immutable
# They cannot be updates
# Tuples are enclosed in ()

a = (14, 'Rohit')
print(a)

b = (25, 85, 9, 2, 0.25)
# b[1] = 52                  # This cannot be done
print(b)
